type Query {
    getBooks: [Book]
    getBook(id: ID!): Book
}

type Book {
    id: ID
    name: String
    price: Int
    author: Author
}

type Author{
    id: ID! # ! indicates it should not be null
    name: String!
    gender: String!
}